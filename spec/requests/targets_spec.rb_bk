require 'spec_helper'

describe "Targets", :type => :request do
      #@user = User.create!(:email=>'admin@abc.com', :password => 'admin123')
  describe "GET /targets" do
    it "should visit the targets index page" do
      #sign_in(@user)
      visit targets_path
      page.should have_content("Login")
      current_path.should == (user_session_path)
      #expect(response).to rediect_to(user_session_path)
      expect(response.status).to be(302)
    end
  end
end
