%h2 Target Statistical Analysis Page(Pie chart)

=javascript_include_tag "//www.google.com/jsapi", "chartkick"

.center{:style =>'left-padding:5px;text-align:center'}
  = "Welcome #{current_user.email}!" |
  = link_to "Sign Out", destroy_user_session_path, :method => :delete

- if !@target.nil?
  = pie_chart({"Targets Reachable" => @target.reachable , "Targets Un-Reachable" => @target.unreachable}) if !@target.nil?
- else
  = pie_chart({"Targets Reachable" => 7 , "Targets Un-Reachable" => 3})
